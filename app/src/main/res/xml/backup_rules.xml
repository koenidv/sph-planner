<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Don't backup the database -->
    <!-- We need to make sure to restore the app when SharedPrefs are backed up, but databases are not -->
    <exclude domain="database" />
    <exclude domain="file" />
</full-backup-content>